\directlua{
  local letter_x = token.create(string.byte("x"))
  local t = token.create(string.byte("x"),12)

  print(t.command)
  print(t.cmdname)
  print(t.csname)
  print(t.id)
  print(t.tok)
  print(t.active)
  print(t.expandable)
  print(t.protected)
  print(t.mode)
  print(t.index)
}

\bye
