test
\directlua{
  print(node.is_glyph(node.new('glyph')))
  print(node.is_glyph(node.new('hlist')))
  callback.register('post_linebreak_filter', function(head)
    local n = head.head
    while (n) do
      print(n)
      print(node.is_glyph(n))
      n = n.next
    end
  end)
}
\bye
