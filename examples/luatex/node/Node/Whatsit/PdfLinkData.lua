local assert = require("utils").assert

assert.node_type("whatsit", "pdf_link_data", {
  id = "whatsit (8)",
  subtype = "pdf_link_data (28)",
  fields = { "prev (-1)", "next (0)", "id (1)", "subtype (2)" },
})
