\directlua{
  callback.register('post_linebreak_filter', function(head)
    print(node.direct.getlist(node.direct.todirect(head)))
    print(node.direct.getlist(node.direct.todirect(head.head)))
  end)
}

Lorem ipsum

\bye
