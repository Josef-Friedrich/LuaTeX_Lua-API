\documentclass{article}
\begin{document}
\catcode`_=12

\directlua{
  info = status.list()
  for key, value in pairs(info) do
    tex.print('"' .. key .. '": '.. tostring(value), '\string\\par')
  end
}
\end{document}
